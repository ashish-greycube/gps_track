<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <!-- NO foreground-service permissions and NO POST_NOTIFICATIONS: this module is
       purely event-driven. The OS wakes the receivers below from a dead state. -->
  <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
  <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
  <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
  <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
  <uses-permission android:name="com.google.android.gms.location.ActivityRecognition" />
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

  <application>
    <!-- Woken by Fused Location batched updates (PendingIntent). Not exported:
         it is triggered only by our own explicit PendingIntent via Play Services. -->
    <receiver
      android:name=".LocationUpdateReceiver"
      android:enabled="true"
      android:exported="false" />

    <!-- Woken by Activity Recognition transitions (STILL / WALKING / IN_VEHICLE). -->
    <receiver
      android:name=".ActivityTransitionReceiver"
      android:enabled="true"
      android:exported="false" />

    <!-- Re-registers the listeners after a device reboot if tracking was active. -->
    <receiver
      android:name=".BootReceiver"
      android:enabled="true"
      android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED" />
        <action android:name="android.intent.action.QUICKBOOT_POWERON" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
    </receiver>
  </application>
</manifest>
